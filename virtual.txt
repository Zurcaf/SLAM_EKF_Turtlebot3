--------------------instalação de ambientes virtuais para python--------------------
pip install virtualenv        #install virtualenv    #fazes uma vez no pc para instalar o virtualenv
virtualenv slam_env           #create virtualenv     #crias um ambiente virtual no git onde estás a trabalhar uma vez

source slam_env/bin/activate  #activate virtualenv    #activas o ambiente virtual para trabalhar sempre

pip install -r requirements.txt

code ~/.bashrc